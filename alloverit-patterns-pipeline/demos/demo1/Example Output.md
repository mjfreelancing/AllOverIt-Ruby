```
  => input = 1, will * 3 via a block
  => input = 3, will increment via a step
  => input = 4, will double via a step
  => input = 8, will square via a step
  => input = 64 Will decrement via a block
Pushed 1 through to a value of 63
```